# Copy to .env and add your key from Anthropic Console
ANTHROPIC_API_KEY=

# Optional: API origin. Leave unset for Vercel (same-origin /api/analyze).
# Local: use Vite proxy (default) or set VITE_API_BASE_URL=http://localhost:3001
# VITE_API_BASE_URL=http://localhost:3001
